import { describe, it } from 'vitest';

describe.skip('navMesh orchestrator integration', () => {
  it('legacy orchestrator module was removed during migration', () => {
    // Covered by controller/navmesh integration tests in active modules.
  });
});
