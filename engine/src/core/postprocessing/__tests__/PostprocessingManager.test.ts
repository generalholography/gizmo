import { describe, it } from 'vitest';

describe.skip('PostprocessingManager', () => {
  it('postprocessing tests are temporarily disabled pending harness rewrite', () => {
    // TODO: re-enable with stable EffectComposer test harness.
  });
});
